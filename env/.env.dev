# TODO : change the default password with the same used in the other .env in intranet-backend/.env
DB_PASSWORD=secret
NODE_ENV=dev
# TODO : Change the default SERVER_NAME with the one you will use like intranet.com or www.intranet.com or 10.12.13.14
# Or even multiple like 10.12.13.14 www.intranet.com my.intranet.fr
# Do not enter the full URL with https:// just the domain or IP
SERVER_NAME=localhost